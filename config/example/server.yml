server:
  rest:
    name: api
    host:
    port: 4011
    tls: false
    timeout: 27
    secretKey: mysecret
    applicationCode: 10
    env: local
groq:
  # SECURITY: never commit real API keys. Example values only. Production
  # secrets live in your secret manager (1Password / Vault / .env.local).
  apiKey: ${GROQ_API_KEY}
  model: openai/gpt-oss-120b
